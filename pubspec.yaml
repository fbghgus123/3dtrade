name: tradeApp
description: A template project with GetX.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get: ^4.6.1
  google_fonts: ^3.0.1
  carousel_slider: ^4.1.1
  kakao_flutter_sdk_user: ^1.2.1
  flutter_dotenv: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  change_app_package_name: ^1.1.0

flutter:
  uses-material-design: true

  assets:
    - asset/
    - asset/images/
    - .env
